# release-checksum

A small Go command-line tool for creating and verifying SHA-256 manifests for
release artifacts. It is designed to use only the Go standard library and to
produce a format compatible with common `SHA256SUMS` files.

The first implementation is tracked in the repository issues.
